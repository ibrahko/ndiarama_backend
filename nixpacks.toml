[phases.setup]
nixPkgs = ["python313", "gcc", "libpq"]

[phases.install]
cmds = ["pip install -r requirements/prod.txt"]

[phases.build]
cmds = ["python manage.py migrate --noinput"]

# collectstatic est dans railway.json startCommand (besoin d'ecrire dans /tmp a l'execution)
